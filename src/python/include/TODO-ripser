Problems:
threshold utilisé à trop d'endroits, mais changer has_next+next pour le prendre en compte est du boulot. fusioner les 2 en un next_raw qui renvoie optional?
num_bits_coef runtime

====

ripser<Complex>
concept Complex {
  using value_t=
  using vertex_t=
  using simplex_t=
  using dimension_t=
  using coefficient_t= (handled as data)
  const int num_coef_bits
  n_vertices()
  get_edges() -> vector<diameter_simplex> qu'on va trier
  boundary()    | range, lazy
  coboundary()  |
}

---

complex<DistanceMatrix,SimplexEncoding>
  // takes existing distmat, but creates simpenc itself

concept DistanceMatrix {
  using value_t=
  using vertex_t=
  // different way to initialize it for each model
  n_vertices() ?
  const bool is_sparse ?
  operator()(vertex1, vertex2)
  neighbors(vertex) -> range<vertex_diameter>
}

// englober CNS et bitfield, mais probablement pas plus
// suppose que le codage d'un simplexe est la somme des codages de (vert,pos)
concept SimplexEncoding {
  using vertex_t=
  using simplex_t=
  ? using dimension_t= (for position)
  ? using coefficient_t= (handled as data)
  const int num_coef_bits
  constructor(n_vertices, max_dim)
  encode(vertex, position) -> simplex
  get_max_vertex(simplex, position) -> vertex (forme faible de decode() qui ne marche que sur le dernier)
  dummy_simplex() / none() // ou utiliser optional à plein d'endroits
}

¿¿ où gère-t-on entry_t (i.e. simplex_t + coefficient_t) ??
class ChainEncoding<SimplexEncoding> {
  using entry_t=
}
ou FiltrationEncoding avec aussi diameter_{simplex,entry} ?


----------------------------
Python
si on passe juste coordonnées:
- cdist
si on passe juste matrice pleine:
- minmax
- si hdim>=2: copie (<=minmax) dans vecteur, edge-collapse, ripser-sparse
- sinon: ripser-dense
si on passe matrice creuse:
- ripser-sparse
si on passe matrice pleine et seuil:
- si hdim>=2: copie (<=seuil) dans vecteur, edge-collapse, ripser-sparse
- sinon: ripser-sparse

Q: compter le nombre d'arêtes avant de décider que sparse est mieux que dense ?
Q: utiliser un kd-tree au lieu de cdist, quand on a un seuil ?

=> pas besoin de gérer directement coordonnées dans ripser. seulement :
* dense, avec seuil fourni (minmax ou utilisateur)
* sparse

- avoir une fonction pour convertir dense+seuil -> sparse
